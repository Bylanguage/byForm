ku byFormNew
{
    new biao identity.config "身份实例配置表" (
    string belong "所属身份实例"
    , string name "名称"
    , string table "表"
    , string flow "数据流")
    {
        { fieldTemplate, select, fieldTemplate,  }
        { fieldTemplate, selectPopup, fieldTemplate,  }
    }
}